# ===================================================================
# DEVELOPMENT ENVIRONMENT CONFIGURATION
# ===================================================================
# This file is loaded in development environment only
# ===================================================================

# Development Database
db.url=jdbc:sqlserver://localhost:1433;databaseName=AISTHEA;encrypt=false
db.username=sa
db.password=12345

# Development Email Settings (use MailHog or similar for testing)
mail.smtp.host=localhost
mail.smtp.port=1025
mail.smtp.auth=false
mail.smtp.starttls.enable=false
mail.from=dev@aisthea.local

# Debug mode
log.level.root=DEBUG
log.level.app=DEBUG

# Base URL for development
app.base.url=http://localhost:8080/FashionProject

# Environment
app.environment=development
